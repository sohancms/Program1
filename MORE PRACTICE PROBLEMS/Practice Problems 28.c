#include <stdio.h>
int main()
{
    int x,y;
    printf("Enter the value of x and y : ");
    scanf("%d %d",&x,&y);
    printf("Display the value of x = %d \nDisplay the value y = %d",x,y);
return 0;
}
