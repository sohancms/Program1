#include <stdio.h>
#include <math.h>
int main()
{
    float n,root;
    printf("Enter the vale of n: ");
    scanf("%f",&n);
    root=sqrt(n);
    printf("The square root result = %.2f",root);
return 0;
}
